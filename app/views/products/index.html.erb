<% if @products.present? %>
  <div class="row justify-content-center mt-3 ms-auto me-auto">
    <%= render(ProductComponent.with_collection(@products))%>
  </div>

  <%= render(PaginationComponent.new(pagy: @pagy)) %>
<% else %>
  <%= render(BlankMessageComponent.new) %>
<% end %>